<%= form_with(model: task, local: true) do |f| %>

  <%= f.label :title, 'タイトル' %>
  <%= f.text_field :title %>
  <%= f.label :content, 'メッセージ' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>